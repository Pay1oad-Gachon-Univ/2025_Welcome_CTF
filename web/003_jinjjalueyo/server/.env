# DB 접속 정보
DB_HOST=db
DB_USER=ctfuser
DB_PASSWORD=ctfpass
DB_NAME=ctfdb

# 관리자 쿠키 값
ADMIN_COOKIE_NAME=ADMIN_TOKEN
ADMIN_COOKIE_VALUE=SOME_SECRET

# 관리자 쿠키 내에 플래그도 함께 있다고 가정 (예: FLAG=CTF{...})
# 보통 document.cookie = "ADMIN_TOKEN=SOME_SECRET; FLAG=CTF{...}" 이런 식
# 여기서는 admin_bot.py에서 두 개의 쿠키로 설정할 수도 있음
FLAG_COOKIE_NAME=FLAG
FLAG_COOKIE_VALUE=Pay1oad{jinja2_stored_xss_exploit_is_fun}

# admin-bot이 접속할 대상 앱 주소
ADMIN_BOT_TARGET="http://ctf_flask_app:5000"
